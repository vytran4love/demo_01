---
- name: Determine the build dir
  set_fact:
    deployed_dir: "{{ ansistrano_release_path.stdout }}"
- name: Run build service "{{ deploy_dir }}"
  shell: |
    yarn install
    yarn build 
  become_user: ubuntu
